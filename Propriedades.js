import React from "react";

export default function Propriedades (props){

    retorn (
        <div>
        <h1 style={{margin:10}}>
            {props.conteudo}
            </h1>
            </div>
    )
}